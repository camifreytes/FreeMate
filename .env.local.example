NEXT_PUBLIC_SUPABASE_URL=https://dkobephentvmxmhyvyir.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_M5S63PurHUZL4QRr7hfpZQ_V-7l43vz

# --- Nuevas, para la integración con Google Calendar ---
# La clave "service_role" de Supabase (Settings -> API). NUNCA la compartas ni la subas a GitHub.
SUPABASE_SERVICE_ROLE_KEY=

# Las mismas credenciales del "Cliente web 1" que ya usa el login con Google (Google Cloud Console -> Clientes)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALENDAR_REDIRECT_URI=https://freemates.app/api/google-calendar/callback

# Vercel la crea sola cuando detecta el cron job (vercel.json). Si no aparece sola
# en Vercel -> Settings -> Environment Variables, agregala vos con cualquier texto.
CRON_SECRET=
